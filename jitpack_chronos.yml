jdk:
  - openjdk21
before_install:
  - sdk install java 21.0.1-open || true
  - sdk use java 21.0.1-open
install:
  - ./gradlew :chronos:clean :chronos:build :chronos:publishToMavenLocal -x test --no-daemon
